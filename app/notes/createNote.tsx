import { View, Text } from 'react-native'
import React from 'react'
import CreateNotes from '../../modules/notes/CreateNotes'

const createNote = () => {
  return (
    <CreateNotes/>
  )
}

export default createNote